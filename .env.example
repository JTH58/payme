# ============================================
# PayMe.tw Environment Variables
# ============================================
# Copy this file to .env.local for local development.
# Most variables are only needed in production (Cloudflare Pages).

# --------------------------------------------
# Cloudflare Pages Build (auto-injected)
# --------------------------------------------
# These are set automatically in the Cloudflare Pages build command.
# Do NOT set them manually in the dashboard.
# NEXT_PUBLIC_COMMIT_SHA=$CF_PAGES_COMMIT_SHA
# NEXT_PUBLIC_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)

# --------------------------------------------
# Cloudflare Dashboard — Secrets
# --------------------------------------------
# Set this in Cloudflare Pages > Settings > Environment variables (Encrypt)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/xxx

# --------------------------------------------
# Cloudflare Dashboard — Bindings
# --------------------------------------------
# Configure these in Cloudflare Pages > Settings > Functions > Bindings:
# - D1 Database: binding name "DB"
# - KV Namespace: binding name "RATE_KV"

# --------------------------------------------
# Hardcoded in next.config.js
# --------------------------------------------
# NEXT_PUBLIC_GITHUB_REPO is set in next.config.js (not an env var).
# Change it there if you fork this project.
